This repository contains the data and the notebook used for the visualization analyses of the work **NIR Spectroscopy and AI to Predict Time and Weight During Cheese Ripening from the Rind**, presented at CYTA CESIA 2026.

## Running the notebook in GitHub Codespaces

This repository is configured to run in GitHub Codespaces.

### Open in Codespaces
1. Open this repository on GitHub.
2. Click **Code**.
3. Open the **Codespaces** tab.
4. Create a new codespace.

When a new codespace is created, the development container will automatically install the required Python dependencies from `requirements.txt`.

## Local setup

If you prefer to run the project locally, create a Python environment and install the dependencies manually:

```bash
pip install -r requirements.txt
```
